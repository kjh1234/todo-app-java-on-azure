@Library('jenkins-shared-library-examples/example1') _

pipeline {
  agent any
  stages {
      stage('lib-call') {
        steps {
          script {
            example1 "Bob"
          }
        }
      }

    }
  }
