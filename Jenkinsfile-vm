pipeline {
  agent any
  stages {
    stage('') {
      steps {
        checkout scm
        sh 'echo \'hi\''
      }
    }

  }
  parameters {
    string(name: 'TAG_VERSION', defaultValue: '', description: '')
  }
}
