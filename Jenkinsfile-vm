pipeline {
  agent any
  stages {
    stage('Test') {
      steps {
        sh 'echo \'hi\''
      }
    }

  }
  parameters {
    string(name: 'TAG_VERSION', defaultValue: '', description: '')
  }
  environment {
    GIT_REPOSITORY_URL = "https://github.com/xxxx/xxxx-backend.git"
    GIT_CREDENTIALS_ID = "XXX-GIT-CREDENTIAL"
    GIT_BRANCH = "*/master"
  }
}
